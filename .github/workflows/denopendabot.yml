name: Update

on:
  workflow_dispatch:
  schedule:
    - cron: "9 0 * * *"

jobs:
  update:
    name: Update
    runs-on: ubuntu-latest
    steps:
      - uses: hasundue/denopendabot@0.5.1
        with:
          token: ${{ secrets.GH_TOKEN }}
